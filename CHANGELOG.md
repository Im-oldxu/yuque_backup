# 更新日志

## v1.3.0 - 2026-07-26

### 更新

- 为每个文档版本保存标准化 Markdown，提供 Markdown 在线读取、Markdown/PDF 下载、可读 `latest`/任务快照导出树及历史版本回填命令。
- 文章页改用经过清理的 Markdown 阅读器，增加本地代码高亮、响应式文章大纲、当前标题跟踪和文档备份状态。
- GitHub Release 增加 Linux amd64/arm64、Windows amd64 原生归档和 SHA256 校验文件。

### 优化

- worker 在成功或部分成功任务结束后生成快照，启动时完成首次回填，并按保留期清理旧快照。
- Docker 镜像补充 PDF 运行库和中文字体，并使用独立的 `yuque-backup-exports` 持久卷保存可读导出树。
- Release 工作流等待同一 Tag、同一提交的普通 CI 成功后再发布原生资产与多架构镜像。

### Bug 修复

- 空白或仅含空白字符的语雀正文不再生成不可读的成功版本，而是记录 `DOCUMENT_BODY_EMPTY` 终态问题。
- 正文候选字段会跳过空白值并继续回退到可用内容，避免有内容的文档被误判为空。

### 版本契约

- OpenAPI 更新为 50 条路径、56 个操作；数据库迁移 head 更新为 `0002`。
- 前后端、统一可执行文件和 Python 包版本同步为 `1.3.0`。

## v1.2.0 - 2026-07-26

### 更新

- 增加包含 Vue 管理端、FastAPI、worker 和 Alembic 的统一可执行文件，提供 `api`、`worker`、`migrate`、`healthcheck` 和版本子命令。
- 增加一体化 Dockerfile、Compose 和运行文档；同一镜像保持迁移、API 与 worker 进程隔离，并由 API 同源提供前端页面。
- 增加 GitHub Actions CI 与 annotated Tag 发布流程，自动验证统一程序和 Compose，并发布 Linux `amd64`/`arm64` GHCR 镜像及 GitHub Release。

### 优化

- 容器改为非 root、只读根文件系统、移除 Linux capabilities，并使用独立数据库/内容命名卷和进程健康检查。
- 简化生产反向代理拓扑，前端页面和 `/api/v1` 不再需要两个独立服务之间的代理配置。
- 补充 Docker 首次运行、日志、备份、从 v1.1.1 bind mount 迁移、升级和回滚说明。

### Bug 修复

- 修复 Windows 本地构建会尝试修改正在使用的开发虚拟环境、导致统一可执行文件构建失败的问题；构建依赖现改为隔离安装。
- 修复只读容器的临时文件系统无法加载 PyInstaller 单文件共享库、导致迁移、API 和 worker 无法启动的问题。

### 验证

- 后端 Ruff、Mypy、`uv lock --check` 和 118 个 Pytest 测试通过；前端 61 个 Vitest 测试、类型检查和生产构建通过。
- Windows 统一可执行文件已通过版本、迁移、嵌入式首页、初始化 API、API ready 和 worker 心跳冒烟验证。
- OpenAPI 保持 47 条路径、53 个操作，数据库迁移 head 保持 `0001`。

## v1.1.1 - 2026-07-26

### 更新

- 无。软件功能与 v1.1.0 相同。

### 优化

- 以新的补丁版本完整补充发布 Tag 的固定说明结构，保留已发布的 v1.1.0 Tag，不覆盖或复用历史 Tag。

### Bug 修复

- 修复 v1.1.0 annotated Tag 注释缺少“新增功能、优化改进、Bug 修复”章节的发布元数据问题。

### 验证

- 重新核对前后端版本文件、OpenAPI 版本、发行包名称和 Compose 镜像标签均为 1.1.1。
- 功能验证基线与 v1.1.0 相同：后端 116 个 Pytest 测试、前端 61 个 Vitest 测试及生产构建均已通过。

## v1.1.0 - 2026-07-26

### 更新

- 增加手动与定时备份向导，可按有效凭据选择一个或多个知识库，并在确认前展示预计 API 请求量和最近额度快照。
- 增加备份额度预估接口、批量知识库任务范围和任务活动快照，任务详情可显示当前文档、附件进度及重试等待。
- 增加凭据编辑能力；修改 Token 或域名时会使旧验证结果失效，并取消受影响的在途备份。

### 优化

- 将定时备份配置并入任务向导，手动备份、定时范围和每日执行时间使用统一流程。
- 收紧资源发现范围，仅下载明确标识的附件；原始 Markdown、图片和普通外链保持原样，安全预览不会主动加载远程图片。
- 额度耗尽后按本地时区持久化等待到次日，管理员仍可手动发起一次额度检查；任务页同时展示安全 URL 和更明确的 TLS 错误提示。
- 改进全局异步错误提示、二级任务导航、移动端布局和无障碍焦点初始化。

### Bug 修复

- 修复取消总任务后，已领取或排队的子任务仍可能继续请求语雀或提交版本的问题。
- 修复任务进度百分比、仪表盘等待额度计数和部分分页总数与公开 API 契约不一致的问题。
- 修复附件 HTTPS 证书错误被归类为可重试网络错误，以及预览重写远程图片链接导致原始内容语义变化的问题。

### 验证

- 后端 Ruff、Mypy、`uv lock --check` 和 116 个 Pytest 测试通过。
- 前端 17 个测试文件、61 个 Vitest 测试、Vue/TypeScript 类型检查和生产构建通过。
- OpenAPI 契约为 47 条路径、53 个操作。

## v1.0.0 - 2026-07-24

### 更新

- 完成 Yuque Backup MVP 前后端，实现唯一管理员认证、多语雀凭据、知识库、文档版本、资源、备份任务、调度、保留清理、墓碑和仪表盘闭环。
- 增加 FastAPI API、独立持久化 worker、SQLite WAL、Alembic 迁移、本地内容存储和后端 Docker Compose。
- 增加 Vue 3 管理端、Mock/真实 API 双模式、响应式桌面与移动端布局，以及 46 条路径、52 个操作的 API 契约实现。

### 优化

- 优化 Token 加密、Argon2id 密码、Session、CSRF、登录限流、Origin 校验、统一异常和敏感信息过滤。
- 优化队列租约、重启恢复、限额等待、任务合并/取消/重跑、原子内容提交、资源去重和保留期清理。
- 优化 HTML 安全清理、SSRF 防护、鉴权下载、版本完整性标记，以及真实 API 模式下的前后端同源代理。
- 更新需求、技术方案、API 契约和前后端快速启动文档，使其与 v1.0.0 代码和部署方式一致。

### Bug 修复

- 修复进程重启后任务租约、取消请求和队列状态无法正确恢复的问题。
- 修复并发 SQLite 读写、会话快照、幂等冲突和异常响应结构相关边界问题。
- 修复文档导航异步竞态、移动端侧栏、Mock 严格校验和真实 API Cookie/CSRF 联调问题。

### 验证

- 后端 99 个测试、Ruff、Mypy、Alembic、wheel/sdist 构建及安装产物迁移通过。
- 前端 11 个测试文件、46 个测试、类型检查、生产构建和桌面/移动端真实浏览器验证通过。
