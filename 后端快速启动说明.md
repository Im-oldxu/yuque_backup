# Yuque-Backup 后端快速启动说明

本文说明 MVP 后端的安装、配置、数据库迁移、API/worker 启动、测试、Python 包和前后端统一可执行文件构建。Docker Compose 运维见 [Docker快速运行项目.md](Docker快速运行项目.md)。所有命令均从项目根目录 `yuque` 开始。

v1.3.0 后端契约为 OpenAPI 50 条路径/56 个操作，数据库迁移 head 为 `0002`。每个版本新增标准化 `export.md`，API 支持 Markdown 读取及 Markdown/PDF 下载，worker 维护可读导出树和快照；后续发布验证由 GitHub CI 在同一 Tag 和提交上执行。

## 1. 运行要求

- Python `3.13.x`，项目约束为 `>=3.13,<3.14`；
- uv `0.10.0` 或兼容版本；
- API 固定单进程，worker 固定单实例；
- SQLite 数据库必须位于本地磁盘，不得放在 NFS/SMB/NAS 等网络文件系统上；
- 内容目录可以使用本地磁盘，也可以使用已验证原子重命名、长文件名和断线行为的 NAS 挂载。

确认工具版本：

```powershell
python --version
uv --version
```

本项目的锁文件以 Python `3.13.12`、uv `0.10.0` 验证。

## 2. 安装依赖

```powershell
cd backend
uv sync --frozen
```

`--frozen` 会严格使用已提交的 `uv.lock`，不会在安装期间改写依赖解析结果。

## 3. 生成主密钥

`APP_MASTER_KEY` 用于 AES-256-GCM 加密语雀 Token，必须解码为恰好 32 字节。下面的命令会生成 URL-safe Base64 值：

```powershell
uv run python -c "import base64,secrets; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
```

每套部署只生成一次并妥善备份。丢失该密钥后，数据库中已有 Token 无法解密；不要把真实密钥提交到 Git、日志或启动说明中。

## 4. 配置环境

在 `backend` 目录创建本地 `.env`：

```powershell
Copy-Item .env.example .env
```

Linux/macOS 可使用：

```bash
cp .env.example .env
```

最小开发配置示例：

```dotenv
APP_MASTER_KEY=<上一步生成的值>
APP_ENV=development
DATA_ROOT=./data
DATABASE_URL=sqlite:///./data/db/yuque-backup.sqlite3
TZ=Asia/Shanghai
SECURE_COOKIES=false
TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
```

配置说明：

- `DATA_ROOT` 下的 `content/` 保存版本、标准化 `export.md`、预览、清单、资源和同文件系统临时目录 `content/.tmp/`；`exports/` 保存可读 `latest`、任务快照与清单；
- `DATABASE_URL` 必须指向本地磁盘上的 SQLite 文件；相对路径以启动命令的工作目录为基准；
- 若内容放在 NAS，可令 `DATA_ROOT=/mnt/nas/yuque-backup`，同时把 `DATABASE_URL` 单独指向本地路径，例如 `sqlite:////var/lib/yuque-backup/db/yuque-backup.sqlite3`；
- Windows 绝对 SQLite URL 使用正斜杠，例如 `sqlite:///D:/yuque-backup/db/yuque-backup.sqlite3`；
- `TRUSTED_ORIGINS` 是允许执行登录和写操作的 Web Origin 列表，不是 API 路径；多个值用逗号分隔；
- 生产环境必须使用 `APP_ENV=production`、`SECURE_COOKIES=true` 和 HTTPS，并把实际 HTTPS Origin 写入 `TRUSTED_ORIGINS`；配置校验会阻止 `APP_ENV=production` 与 `SECURE_COOKIES=false` 的组合，HTTPS 终止和 Origin 是否与外部入口一致需由部署者确认。

API 与 worker 必须使用完全相同的 `APP_MASTER_KEY`、`DATA_ROOT`、`DATABASE_URL` 和 `TZ`。

## 5. 初始化或升级数据库

先执行 Alembic 迁移：

```powershell
uv run yuque-backup-migrate
```

确认数据库位于当前迁移 head，并检查模型与迁移是否仍一致：

```powershell
uv run alembic current --check-heads
uv run alembic check
```

API 和 worker 都会在启动时检查迁移版本。数据库未到 head 时会拒绝启动，不会自动建表或静默修改结构。

v1.3.0 的当前迁移 head 为 `0002`。从旧版本升级并完成迁移后，可执行一次历史 Markdown 回填和可读导出树重建：

```powershell
uv run yuque-backup export
```

该命令要求数据库已经位于当前 head；它不会请求语雀，会为未清理的历史版本补写 `export.md`、重建 `DATA_ROOT/exports/latest`，并在尚无快照时创建首次回填快照。

## 6. 启动 API 和 worker

终端一，在 `backend` 目录启动单 API 进程：

```powershell
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
```

终端二，在同一 `backend` 目录启动唯一 worker：

```powershell
uv run python -m app.worker.main
```

不要启动多个 API worker 或多个后台 worker。MVP 的 SQLite 协调、全局 Cron 和单总任务约束均按“一个 API 进程、一个 worker”设计。

开发环境的 OpenAPI 页面位于：

```text
http://127.0.0.1:8000/docs
```

生产环境默认关闭该页面。

## 7. 健康检查和首次初始化

进程存活检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health/live
```

数据库、迁移和内容目录就绪检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health/ready
```

预期分别返回：

```json
{"status":"ok"}
{"status":"ready"}
```

查看是否已创建唯一管理员：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/system/initialization
```

首次初始化、登录和后续写操作需要符合《API 接口文档.md》的 `Origin`、会话 Cookie、CSRF 和 `Idempotency-Key` 要求。不要在反向代理中删除这些请求头或 `Set-Cookie`。

## 8. 运行测试和质量检查

```powershell
uv run ruff check app tests
uv run mypy app
uv run pytest -q
uv lock --check
```

迁移往返检查已包含在 `tests/test_migrations.py`；它使用临时数据库，不会操作 `.env` 指向的业务数据库。

## 9. 构建产物

### 9.1 构建和验证 wheel

```powershell
uv build
```

产物位于 `backend/dist/`：

```text
yuque_backup_backend-1.3.0-py3-none-any.whl
yuque_backup_backend-1.3.0.tar.gz
```

wheel 内含 Alembic 配置和迁移脚本。安装后可使用三个入口：

```powershell
yuque-backup-migrate
yuque-backup-api
yuque-backup-worker
```

运行安装产物时，需要在当前目录提供 `.env`，或在进程环境中显式设置同一组变量。必须先运行 `yuque-backup-migrate`，再启动 API 和 worker。

### 9.2 构建前后端统一可执行文件

统一构建会依次执行锁定的前端生产构建和 PyInstaller 单文件构建，不修改业务依赖版本：

```powershell
python scripts/build_unified.py
```

已执行过 `pnpm install --frozen-lockfile` 时可跳过重复安装：

```powershell
python scripts/build_unified.py --skip-install
```

Windows 产物为 `backend/dist/yuque-backup.exe`，Linux 产物为 `backend/dist/yuque-backup`。它内含 Vue 生产资源、FastAPI API、worker、Alembic 配置和迁移脚本，可使用：

```text
yuque-backup migrate
yuque-backup export
yuque-backup api
yuque-backup worker
yuque-backup healthcheck api
yuque-backup healthcheck worker
yuque-backup --version
```

必须保持一个 API 进程和一个 worker 进程。升级时先执行 `migrate`，需要立即回填历史可读导出时再执行一次 `export`；worker 启动也会准备缺失的首次回填。统一可执行文件是统一的发布载体，不会把长时 worker 工作合并进 API 进程。

## 10. Docker Compose

最终镜像只分发上述统一可执行文件。Compose 用同一镜像的 `migrate`、`api` 和 `worker` 子命令保持原有进程隔离，详细步骤见 [Docker快速运行项目.md](Docker快速运行项目.md)。

## 11. 常见启动失败

- `APP_MASTER_KEY must decode to exactly 32 bytes`：主密钥不是 32 字节的 URL-safe Base64 或 64 位十六进制；
- `database schema is not at the Alembic head`：先运行 `uv run yuque-backup-migrate`；
- `unable to open database file`：数据库父目录不存在、不可写或位于不支持 SQLite WAL 的网络文件系统；
- `/health/ready` 返回 `503`：检查数据库写权限、迁移 head 和内容目录是否存在且可访问；
- 登录或写操作返回 Origin/CSRF 错误：确认 `TRUSTED_ORIGINS` 与浏览器实际 Origin 完全一致，并允许 Cookie 与 CSRF 请求头通过代理；
- worker 显示离线：确认独立 worker 进程正在运行，并与 API 使用同一数据库；
- worker 无法写内容：确认 `DATA_ROOT/content` 与 `content/.tmp` 在同一文件系统且运行用户可写。

不要通过删除 SQLite 文件、迁移版本表或内容目录来“修复”已有部署；先保留数据副本，再依据日志和健康检查定位权限、迁移或配置问题。
