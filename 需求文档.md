# Yuque-Backup（语雀备份）MVP 需求文档

> 文档状态：已确认，可进入设计与开发评审  
> 版本：MVP v1.0  
> 调研及需求确认日期：2026-07-23  
> 目标部署方式：Docker Compose，本地服务器或已挂载 NAS 的宿主机  
> 本文档范围：产品调研、MVP 需求、约束、概念数据模型及验收标准；不包含项目代码

## 1. 项目理解

Yuque-Backup 是部署在用户自己服务器上的“语雀只读保险箱”。系统使用一组或多组语雀 Token，通过语雀官方 OpenAPI 定时拉取 Token 有权访问的知识库、目录、文档正文以及能够下载的图片和附件，并保存到本地持久化目录。

当语雀因网络、账号异常或服务故障而无法访问，或者语雀中的文档被误改、误删时，管理员仍可以登录本系统，按知识库目录浏览已经备份的内容、查看历史版本、查询每次备份任务，并下载原始正文、离线 HTML 和已保存的附件。

本系统是单向、只读备份系统，不是双向同步工具，也不是语雀替代编辑器。MVP 不调用任何语雀写接口，不把本地历史版本恢复或覆盖到语雀。

## 2. 结论摘要

### 2.1 需求合理性

该需求合理，且现有开源工具没有完整覆盖目标场景。

现有语雀导出工具通常解决“一次性导出为 Markdown”问题，部分工具支持图片或附件下载、简单增量和本地静态预览，但普遍缺少以下系统级能力：

- 多组凭据的统一请求调度和知识库去重；
- 对共享 API 额度的持续感知、暂停和自动恢复；
- 持久化任务队列、失败重试和容器重启后的断点续传；
- 文档变化版本、删除保留、保留期清理和资源引用回收；
- 面向非命令行用户的任务记录、错误明细和本地 Web 浏览；
- 对普通文档、表格、数据表、话题、图集和 HTML 文档的统一降级策略；
- 对“正文成功但部分资源失败”的可观测状态。

因此，Yuque-Backup 的产品价值不在于重新实现一个 Markdown 下载器，而在于把导出能力提升为可长期运行、可审计、可恢复访问的本地备份系统。

### 2.2 MVP 核心能力

MVP 必须优先实现以下闭环：

1. 初始化唯一管理员并安全登录；
2. 配置、验证和管理多组语雀凭据；
3. 发现凭据可访问的知识库，并选择备份范围；
4. 通过统一队列完成首次备份和定时增量备份；
5. 在限流、网络失败或应用重启后自动续传；
6. 保存文档版本、可获取的图片和附件，并准确显示不完整项；
7. 在本地 Web 中浏览知识库、预览版本、查询任务和下载资料；
8. 按保留期清理历史和已删除文档，同时保留审计墓碑。

## 3. 调研方法与证据分级

本次调研使用 Firecrawl MCP 检索和抓取语雀官方文档、竞品页面及 GitHub 项目，并通过登录后的语雀官方文档页取得和结构化解析 `yuque_openapi_20260722.yaml`。GitHub 项目的许可证、更新时间和目录结构另以仓库元数据及原始文件交叉核验。

本文使用以下证据等级：

| 标记 | 含义 | 使用规则 |
| --- | --- | --- |
| 官方明确 | 语雀官方页面或 `yuque_openapi_20260722.yaml` 直接规定 | 可作为功能设计和验收依据 |
| 社区验证 | 开源项目源码、README 或社区实践显示可行，但官方规范未承诺 | 只能作为兼容性方案；失败时必须降级并告警 |
| 产品决策 | 根据已确认的 MVP 范围、竞品模式和工程风险制定 | 属于本项目需求，不代表语雀官方行为 |

任何“社区验证”的接口、Cookie 或 URL 转换规则都不得在界面、文档或验收标准中描述为语雀官方能力。

## 4. 语雀官方 API 调研

### 4.1 认证与使用资格

**官方明确：**

- 所有开放 API 使用 HTTP Header `X-Auth-Token` 鉴权。
- Token 能访问的数据取决于其实际权限；系统不得假设公开分享内容一定可访问。
- 个人 Token 是语雀超级会员权益。
- 空间内团队 Token 是旗舰版空间权益。
- 访问空间内资源时，需要使用对应空间子域名，而不一定是 `https://www.yuque.com`。
- 从 2022-04-10 起，个人 Token 只能访问 Token 有权访问的内容，不能借助开放 API 任意读取公开文档。
- 时间字段使用 ISO 8601，通常为 UTC。

**产品含义：**

- 凭据配置必须包含显示名称、API 基础域名和 Token；系统通过实际响应识别个人或团队主体。
- 系统只验证 Token 的实际可用性，不代购会员、不绕过权限、不使用浏览器 Cookie 获取额外内容。
- 无权访问的知识库、文档或附件属于来源权限限制，不属于系统数据丢失。

### 4.2 调用频率与额度

**官方明确：**

- 每小时最多 5000 次请求；
- 每秒最多 100 次请求；
- 个人或团队下的所有 Token 共享同一个使用次数限制；
- 响应提供 `X-RateLimit-Limit` 和 `X-RateLimit-Remaining`；
- 请求过于频繁时可能需要稍后重试；
- OpenAPI 对 429 的定义仅为“访问频率超限”。

**官方未明确：**

- 没有规定额度恢复时间响应头；
- 没有保证返回 `Retry-After`；
- 没有提供可依赖的 `X-RateLimit-Reset`；
- 不同会员等级是否存在更低的额外日额度，没有在本次取得的 OAS 中定义。

**产品含义：**

系统不能依赖固定整点或固定日界恢复额度，必须持续读取响应头，并为 429 提供可持久化的保守退避和自动探测机制。

### 4.3 与备份相关的只读接口

以下路径来自 `yuque_openapi_20260722.yaml`，OpenAPI 版本为 3.1.0，接口文档版本为 2.0.1。

| 用途 | 方法与路径 | 关键参数/返回 | MVP 用法 |
| --- | --- | --- | --- |
| 验证服务 | `GET /api/v2/hello` | 标准错误码含 401、403、429 | 可用于低成本连通性检查 |
| 当前主体 | `GET /api/v2/user` | 用户/团队的 ID、type、login、name | 验证 Token 并识别主体 |
| 用户所在团队 | `GET /api/v2/users/{id}/groups` | `offset`、团队信息 | 个人 Token 发现团队 |
| 个人知识库 | `GET /api/v2/users/{login}/repos` | `offset`、`limit<=100`、`type` | 发现个人知识库 |
| 团队知识库 | `GET /api/v2/groups/{login}/repos` | `offset`、`limit<=100`、`type` | 发现团队知识库 |
| 知识库详情 | `GET /api/v2/repos/{book_id}` | name、slug、namespace、更新时间 | 保存知识库元数据 |
| 知识库目录 | `GET /api/v2/repos/{book_id}/toc` | `V2TocItem[]` | 构建目录树 |
| 文档列表 | `GET /api/v2/repos/{book_id}/docs` | `offset`、`limit<=100`、`deleted`、`changed_at_gte`、`optional_properties` | 首次、增量和删除同步 |
| 文档详情 | `GET /api/v2/repos/docs/{id}` | `page`、`page_size<=200`；正文与类型字段 | 获取当前正文；分页获取数据表 |
| 文档历史列表 | `GET /api/v2/doc_versions?doc_id={id}` | 最近 100 个已发布版本，按时间倒序 | 记录远端版本信息；MVP 首次不回补 |
| 文档历史详情 | `GET /api/v2/doc_versions/{id}` | 历史正文、HTML、Markdown/Lake、diff | 后续兼容能力；不作为无限历史保证 |

路径还存在按 `group_login/book_slug` 重载的版本。MVP 内部优先使用稳定的数值 `book_id` 和 `doc_id`；只有在数值路径不可用或需要兼容时才使用路径重载。

### 4.4 增量和删除能力

**官方明确：**

- 文档列表支持 `changed_at_gte`，格式为 ISO 8601；
- 文档摘要包含 `updated_at` 和 `content_updated_at`；
- 通过 `optional_properties=latest_version_id` 可取得最新已发布版本 ID；
- 文档列表支持 `deleted=true`；
- 已删除视图返回 `deleted_at` 和 `deleted_slug`；
- 知识库详情含 `content_updated_at`；
- 文档 ID、知识库 ID 可作为稳定身份，不应使用标题作为主键。

**MVP 同步策略：**

- 首次任务分页获取当前文档，不回补部署前历史版本；
- 后续任务使用上次安全水位减去 5 分钟作为 `changed_at_gte`，通过内容哈希去重，避免时间边界漏项；
- 每轮同时查询当前变化和删除变化；
- 标题、slug 或目录位置变化只更新同一文档，不创建新文档；
- 只有在变化列表枚举成功，且变化项已提交或已进入持久化重试队列后，才能推进安全水位。

### 4.5 文档类型和正文

| 类型 | 官方说明 | MVP 保存与预览策略 |
| --- | --- | --- |
| `Doc` | 普通文档 | 保存原始字段；优先清理并渲染 `body_html` |
| `HtmlDoc` | HTML 文档 | 保存原始 HTML；安全清理后离线预览 |
| `Thread` | 话题 | 优先使用 `body_html`；不可渲染时提供原始正文和告警 |
| `Board` | 图集 | 优先使用 `body_html`；内部对象无法离线化时降级 |
| `Sheet` | 语雀表格 | 解析 JSON 字符串 `body_sheet`，按工作表只读展示 |
| `Table` | 数据表 | 使用 `body_table`，按 `page/page_size` 拉取全部记录并只读展示 |

文档详情的 `format` 官方枚举为 `markdown`、`lake`、`html`、`lakesheet`。相关正文可能出现在 `body`、`body_html`、`body_lake`、`body_sheet`、`body_table` 等字段中。系统必须保存 API 原始响应和可读的标准化结果，不能只保留渲染后的 HTML。

数据表中的文件和图片单元格可包含 `name`、`src`、`size` 等资源信息，应纳入资源发现。数据表单页 `page_size` 最大为 200；任务必须持续翻页，直至取得 `totalCount` 指示的全部记录或遇到明确错误。

### 4.6 图片和附件限制

**官方明确：**

- 文档 HTML 中的图片以 URL 形式存在；
- 数据表文件或图片字段可以包含资源 URL；
- 当前 OAS 的 23 个路径中没有附件枚举或附件下载接口。

**社区验证：**

- 多个导出项目通过解析 Markdown、HTML、Lake 或数据表中的 URL 下载图片；
- `M1r0ku/YuqueExport` 将部分 `/attachments/` URL 转换为 `/api/v2/attachments/` 请求；
- `gxr404/yuque-dl` 通过登录 Cookie 下载部分受保护附件。

**MVP 边界：**

- 从官方正文和结构化字段中发现资源；
- 优先直接请求正文给出的 URL，并在适用时携带当前 Token；
- 不使用浏览器 Cookie，不要求用户提交语雀会话 Cookie；
- 未公开 URL 转换只能放在隔离的兼容适配器中，并且必须先对当前域名验证；
- 任一兼容尝试失败时保留正文，记录“资源未备份”，不得将整篇正文回滚；
- 产品对“可获取的图片和附件”提供尽力备份，不承诺官方权限之外的完整性。

## 5. 竞品调研

### 5.1 产品对比

| 产品/能力 | 观察到的模式 | 对 MVP 的借鉴 | MVP 不采用 |
| --- | --- | --- | --- |
| ProBackup | 自动日备份、快照历史、细粒度恢复、状态与告警 | 自动调度、历史时间线、资源级错误可见 | 多 SaaS 连接器、远端一键恢复、企业套餐体系 |
| Rewind Backups | 初始全量后增量、按日期浏览、单项或整体恢复 | 首次与增量状态区分、版本按日期浏览 | 回写语雀、整实例恢复、SSO/RBAC |
| SimpleBackups | 自动计划、备份目标与任务历史，强调原生备份不足 | 调度可见、失败任务可追踪、数据独立持有 | 云目标市场、多云存储配置 |
| Notion 原生备份 | 页面历史、回收站和手动导出各自覆盖部分场景 | 区分“版本”“删除保留”“独立导出”三个概念 | 假设 SaaS 自身历史等同于独立备份 |

### 5.2 可借鉴的交互模式

- 首屏直接展示最近备份结果和需要处理的问题，而不是营销信息；
- 用“成功、部分成功、失败、等待额度”表达备份可信度；
- 历史以日期和任务为主线，不要求用户理解 Git 提交；
- 支持从任务下钻到凭据、知识库、文档和资源错误；
- 支持单篇内容和单个附件下载，不把恢复能力隐藏在整库操作中；
- 初次配置尽量通过向导完成，但不淡化 Token 权限、会员资格和附件限制。

## 6. 开源项目调研与许可证

> 下表的 Star、Fork 和最后推送时间为 2026-07-23 调研快照，仅用于判断社区规模和维护状态，不代表质量保证。

| 项目 | 调研快照 | 相关能力 | 许可证结论 | MVP 处理 |
| --- | --- | --- | --- | --- |
| `gxr404/yuque-dl` | 2289 Star、339 Fork；最后推送 2026-06-27 | 批量下载、进度文件、增量检查、断点续传、图片/附件、VitePress 预览 | 仓库未声明许可证；默认没有复制、修改或分发许可 | 只借鉴功能和交互思路，禁止复制代码 |
| `atian25/yuque-exporter` | 366 Star、43 Fork；最后推送 2024-05-10 | TOC 目录构建、图片下载、链接重写、Markdown 导出 | MIT | 可按文件评估复用，并保留版权和 MIT 声明 |
| `M1r0ku/YuqueExport` | 94 Star、13 Fork；最后推送 2026-01-24 | 异步图片下载、附件 URL 识别、API 节流等待 | MIT | 可复用通用解析/下载逻辑；未公开接口必须隔离和验证 |
| `Tencent/WeKnora` | 18795 Star、2628 Fork；最后推送 2026-07-23 | 语雀分页、重试、`Retry-After`、增量游标、删除检测 | 主体 MIT；LICENSE 明确提示第三方组件可能使用不同条款 | 只选择语雀连接器相关文件；复用前逐文件和依赖审计 |
| `yuque-helper/yuque2book` | 431 Star、49 Fork；最后推送 2026-02-12 | 目录化导出、本地资源、图书式输出 | SATA，非标准宽松许可证并附带额外义务 | 不复用代码 |

### 6.1 复用原则

1. 核心数据模型、任务状态机、持久化队列和保留清理自行实现；
2. 仅在能显著降低风险时，按文件选择性复用 MIT 代码；
3. 每个复用文件记录来源仓库、提交、许可证和修改说明；
4. 不整包引入导出工具、WeKnora AI/RAG 平台或其无关依赖；
5. 不从无许可证或 SATA 项目复制源码、测试、模板或文档段落；
6. 发布前输出第三方依赖清单并完成许可证扫描；
7. 社区项目中的未公开语雀接口不得写入公共核心接口层。

## 7. 产品目标与成功标准

### 7.1 产品目标

- 在语雀不可访问时，管理员仍能浏览已经成功备份的知识库和文档；
- 在文档误改后，可查看保留期内的本地历史版本；
- 在文档误删后，可在默认 15 天窗口内查看和下载最后成功副本；
- 在 API 限流、短期网络失败或容器重启后，任务能自动继续而不重复产生版本；
- 对无法取得的资源明确告警，不用“成功”掩盖不完整备份；
- Token、私有正文和附件只保存在用户控制的服务器中。

### 7.2 MVP 成功标准

- 所有语雀写操作为零；
- 所有 OpenAPI 请求均经过统一调度器；
- 相同内容不会因重复任务生成重复版本或重复物理资源；
- 已提交的最后成功版本不会因后续失败被覆盖；
- 任务状态可以解释“备份了什么、遗漏了什么、为什么遗漏”；
- 断开语雀网络后，已备份文档、离线 HTML 和本地资源仍可访问和下载；
- Docker Compose 重启后，配置、任务、检查点和内容保持一致。

## 8. 用户与核心场景

### 8.1 用户

MVP 只有一个本地管理员。管理员负责：

- 首次初始化和密码管理；
- 配置语雀 Token 和空间域名；
- 选择知识库及重复知识库的主凭据；
- 设置 Cron、时区、保留期和资源大小上限；
- 触发、取消和查看备份任务；
- 浏览、搜索、预览和下载已备份内容。

### 8.2 核心场景

1. **首次部署：** 管理员创建本地账号，添加凭据，验证后选择知识库，手动执行首次备份。
2. **日常增量：** Cron 每天触发，各凭据只拉取变化和删除项，未变化文档不重复请求详情。
3. **额度耗尽：** 某凭据进入等待额度状态，其他凭据继续；额度恢复后自动续传。
4. **误改恢复访问：** 管理员在版本时间线中查看旧内容并下载，不回写语雀。
5. **误删恢复访问：** 管理员在删除窗口内找到文档最后副本、查看剩余保留时间并下载资料。
6. **语雀不可用：** 系统不依赖语雀即可完成登录、目录浏览、标题搜索、版本预览和本地下载。
7. **部分资源失败：** 正文仍可查看，页面和任务清楚列出未下载资源及原因。

## 9. MVP 功能范围与优先级

| 优先级 | 功能 | 说明 |
| --- | --- | --- |
| P0 | 初始化与登录 | 唯一管理员、关闭注册、修改密码、安全会话 |
| P0 | 多凭据管理 | 新增、验证、更新、启停、脱敏、错误状态 |
| P0 | 知识库发现与选择 | 默认全选、允许排除、重复库指定主凭据 |
| P0 | 全局 Cron 与手动任务 | 统一调度、单总任务、重复触发合并 |
| P0 | 全量/增量/删除同步 | 当前版本、变化列表、删除视图、目录同步 |
| P0 | 限流、重试和断点续传 | 429 等待、持久化退避、原子提交、检查点 |
| P0 | 本地版本和资源 | 原始响应、原始正文、安全 HTML、SHA-256 资源 |
| P0 | 知识库浏览和文档预览 | 目录树、六种类型降级、历史时间线 |
| P0 | 任务与问题明细 | 状态、筛选、下钻、凭据/文档/资源错误 |
| P0 | 保留和清理 | 默认 15 天、最新版本保护、墓碑、引用回收 |
| P1 | 标题与路径搜索 | 不索引正文，仅用于快速定位文档 |
| P1 | 单篇多格式下载 | 原始正文、离线 HTML、单个附件 |
| P1 | 存储统计 | 数据库、正文和资源用量展示 |

P1 仍属于 MVP 交付范围，但可以在 P0 闭环稳定后实现。

## 10. 详细功能需求

### 10.1 初始化、登录和安全

#### FR-AUTH-001 首次初始化

- 数据库中不存在管理员时，访问系统进入初始化向导；
- 向导只创建一个管理员，提交操作必须具备并发幂等性，只有第一个有效请求成功；
- 初始化完成后，注册/初始化接口永久关闭，除非数据库被显式重建；
- 密码不得以明文保存，应使用适合密码的慢哈希；
- 管理员密码最少 12 个字符。

#### FR-AUTH-002 登录与会话

- 提供登录、退出和修改密码；
- 登录使用服务端会话和 `HttpOnly` Cookie；生产部署由反向代理提供 HTTPS；
- 连续失败登录需要限速，错误信息不得透露账号是否存在；
- 修改密码后使其他已存在会话失效；
- MVP 不提供自助找回密码、多因素认证、注册、多用户或角色。

#### FR-AUTH-003 Token 安全

- Token 使用部署密钥加密后保存；部署密钥不得存入数据库；
- Token 在页面仅显示末尾少量字符，任何接口和日志不得返回完整值；
- 任务错误中的请求头必须脱敏；
- 导出需求数据、任务日志或诊断信息时不得包含 Token。

### 10.2 多凭据管理

#### FR-CRED-001 凭据字段

每组凭据至少包含：显示名称、API 基础域名、加密 Token、主体 ID、主体类型、login、验证状态、启用状态、最后验证时间、额度头快照、暂停原因和下一次尝试时间。

#### FR-CRED-002 凭据验证

- 保存前调用 `GET /api/v2/user` 验证；
- 识别个人或团队主体；
- 区分网络失败、域名错误、401、403、429 和其他服务端错误；
- 验证失败时允许保存为“需要处理”，但不能加入自动备份；
- 更新 Token 后需要重新验证。

#### FR-CRED-003 启停和删除

- 停用凭据后不再发出请求，正在等待的子队列暂停；
- 删除凭据前显示其关联知识库；
- 删除或停用凭据不得删除已有知识库、版本、资源或任务历史；
- 没有其他可用凭据的知识库显示“连接已停用”。

### 10.3 知识库发现、选择和去重

#### FR-REPO-001 发现流程

- 个人主体：获取个人知识库、所在团队，再获取各团队知识库；
- 团队主体：直接获取该团队知识库；
- 分页大小使用官方最大值 100；
- 默认发现 `Book` 类型，同时记录其他知识库类型为不支持或待评估，不静默遗漏。

#### FR-REPO-002 范围选择

- 新发现知识库默认选中；
- 管理员可以排除知识库；
- 取消选择只停止未来备份，不删除已有内容；
- 重新选中后从现有安全水位继续，必要时补做当前状态核对。

#### FR-REPO-003 重复知识库

- 使用规范化 API 域名和 `book_id` 作为知识库唯一键；
- 多组凭据发现同一知识库时只创建一个浏览入口；
- 管理员必须指定主凭据，备用凭据默认不发请求；
- 主凭据失效时系统提示切换，不自动使用可能权限不同的备用凭据；
- 切换凭据后保留原有文档和版本身份。

### 10.4 调度和任务

#### FR-JOB-001 Cron

- 支持标准五段 Cron；
- 默认 `0 2 * * *`；
- 默认 IANA 时区 `Asia/Shanghai`；
- 保存前校验表达式和时区，并展示下三次计划时间；
- Cron 触发所有已启用且至少选择一个知识库的凭据。

#### FR-JOB-002 手动触发

- 可触发全部、单组凭据或单个知识库；
- 手动任务与 Cron 使用同一个调度器和状态模型；
- 已有任务运行时，新的触发进入一个待执行槽位；相同范围去重，不同范围合并为并集。

#### FR-JOB-003 单总任务约束

- 全局只允许一个 `BackupJob` 处于运行中；
- 一个总任务可包含多组凭据子任务；
- 不同凭据子任务可交错执行，但每组凭据最多一个 OpenAPI 请求在途；
- 任何控制器、定时器或资源下载器都不得绕过统一调度器直接调用 OpenAPI。

#### FR-JOB-004 取消和续跑

- 管理员可取消运行或等待额度的任务；
- 取消不删除已成功提交的数据和检查点；
- 当前原子写入单元完成或安全中止后停止；
- 后续重新执行从检查点继续，不提供单独的“恢复旧任务”按钮。

### 10.5 API 调度、额度和重试

#### FR-QUEUE-001 持久化队列

每个请求记录：凭据、任务、子任务、知识库、文档、请求类别、优先级、状态、尝试次数、最后错误、`next_retry_at` 和检查点关联。应用重启后能够重建未完成队列。

#### FR-QUEUE-002 主动限速

- 每组凭据默认最多一个 OpenAPI 请求在途；
- 默认平均速率不高于每秒 1 次，以低于官方每小时上限运行；
- 每次响应更新 `X-RateLimit-Limit` 和 `X-RateLimit-Remaining` 快照；
- `Remaining` 接近或等于零时主动进入等待，避免持续制造 429；
- 因语雀端共享 Token 额度，不能通过增加本系统凭据并发来规避限额。

#### FR-QUEUE-003 429

- 仅暂停命中 429 的凭据子队列，其他凭据继续；
- 若存在可解析的 `Retry-After`，优先采用；
- 否则等待时间从 60 秒开始指数增加，最大 1 小时；达到上限后每小时探测一次；
- 不设自动失败次数上限，直至请求成功、任务取消或凭据停用；
- 状态和 `next_retry_at` 必须持久化；
- 页面显示“等待额度”和预计下次尝试时间，不显示为普通失败。

#### FR-QUEUE-004 可重试错误

- 网络错误、408、连接超时和 5xx 最多重试 3 次；
- 重试间隔依次为 2 秒、10 秒和 30 秒；
- 耗尽后记录单项失败，继续处理其他文档；
- 同一错误不得形成无界快速循环。

#### FR-QUEUE-005 不可重试或需人工错误

- 401/403：停止对应凭据子任务，凭据标记“需要处理”；
- 404：若发生在文档详情，重新核对删除视图；确认删除则写墓碑，否则记录来源不一致；
- 422：保存参数和响应摘要，标记实现或来源兼容问题，不盲目重试；
- 磁盘空间不足：停止所有新提交，保护已存在的最后成功版本。

### 10.6 同步和版本

#### FR-SYNC-001 首次备份

- 保存知识库详情、目录、当前文档列表和当前文档详情；
- 不调用历史详情批量回补部署前版本；
- 记录远端 `latest_version_id`，供后续变化判断和审计；
- 每个知识库首次完成后写安全水位。

#### FR-SYNC-002 增量备份

- 每轮同步知识库元数据和目录；
- 使用安全水位减 5 分钟查询 `changed_at_gte`；
- 通过 `doc_id` 和内容哈希消除重叠结果；
- 只为变化或本地缺失文档请求详情；
- 同时查询 `deleted=true` 的变化；
- 资源失败不阻塞文档变化水位，但必须进入独立持久化重试/问题记录。

#### FR-SYNC-003 版本触发

以下任一内容变化时创建新版本：原始正文、标题、slug、文档类型、影响预览的元数据、目录路径、资源清单或资源引用。完全相同的规范化版本哈希不得重复存储。

#### FR-SYNC-004 版本内容

每个版本至少保存：

- 语雀文档 ID、知识库 ID 和远端版本 ID；
- API 原始响应 JSON；
- 原始正文及原始格式；
- 标准化标题、路径、目录位置和时间字段；
- 清理后的离线 HTML；
- 版本哈希、完整性状态和来源任务；
- 资源清单及各资源状态；
- 创建本地版本的时间。

#### FR-SYNC-005 提交规则

- 文件先写入同一文件系统内的临时路径；
- 完成大小/哈希校验后原子重命名；
- 数据库引用只在文件提交成功后生效；
- 文档版本或资源完成后立即更新检查点；
- 失败版本不得替换 `latest_successful_version_id`；
- “部分成功”正文可以成为最新成功正文，但必须携带不完整标记和问题清单。

### 10.7 文档预览

#### FR-PREVIEW-001 通用 HTML 安全

- 删除 `script`、可执行嵌入、内联事件处理器和危险协议；
- 外部链接保留但标识为外部地址；
- 已成功下载的资源 URL 改写为受本系统权限保护的本地 URL；
- 未本地化资源不得在后台静默访问远端，应显示缺失占位和原因；
- 代码高亮由本地静态资源完成，不依赖语雀 CDN。

#### FR-PREVIEW-002 普通类型

- `Doc`、`HtmlDoc`、`Thread`、`Board` 优先使用 `body_html`；
- 没有可用 HTML 时，可从官方原始正文生成安全预览；
- 无法转换时显示元数据、原始格式说明、下载入口和告警，而不是空白页。

#### FR-PREVIEW-003 Sheet

- 解析 `body_sheet` JSON；
- 支持多个工作表切换；
- 表格只读，展示行列数据；
- 解析失败时保留原始 JSON 下载并标记部分成功。

#### FR-PREVIEW-004 Table

- 按 `page_size=200` 分页获取，直至达到 `totalCount`；
- 根据 meta/columns 顺序展示记录；
- 图片和文件列进入资源发现；
- 不实现编辑、公式重算、筛选器设计器或语雀特有交互；
- 任一分页失败时保存已取得的原始分页数据，并将版本标记为部分成功。

### 10.8 图片和附件

#### FR-ASSET-001 资源发现

从 Markdown、HTML、Lake、Sheet、Table 和其他 API 字段中提取候选 URL；保存原始 URL、标准化 URL、名称、MIME、声明大小、来源位置和资源类型。

#### FR-ASSET-002 下载

- 使用流式下载，不把整个大文件载入内存；
- 默认单资源上限 500 MB；管理员可改为其他正数或无限制；
- 有 `Content-Length` 时在下载前判断；没有时在流式读取达到上限时安全中止；
- API 请求和资源下载使用独立有界并发池；资源下载不得占用其他凭据的 OpenAPI 单请求通道；
- 下载完成后计算 SHA-256，使用内容地址保存。

#### FR-ASSET-003 去重和引用

- 相同 SHA-256 只保存一个物理文件；
- 版本通过 `VersionAsset` 引用资源；
- 不同原始 URL 指向相同内容时允许共享；
- 清理版本时减少引用计数，只有引用为零才删除物理资源。

#### FR-ASSET-004 失败和部分成功

以下情况生成 `BackupIssue`：超出大小限制、401/403、404、链接过期、超时、重试耗尽、格式无法识别、兼容适配器失败。问题记录不得包含 Token，并应保存可安全展示的 URL、原因、尝试次数和最后时间。

### 10.9 删除、保留和清理

#### FR-RET-001 保留策略

- 默认 15 天；
- 管理员可设置正整数天数；
- 修改后从下一次清理任务开始使用新值；
- 活动文档的最新成功版本不受保留期清理；
- 其他历史版本按本地版本完成时间计算过期。

#### FR-RET-002 已删除文档

- 以官方 `deleted_at` 作为删除计时起点；
- 保留期内仍可浏览、搜索和下载；
- 页面显示原路径、删除时间和预计清理时间；
- 到期后清理所有正文版本及无共享引用资源。

#### FR-RET-003 墓碑

正文清理后永久保留：域名、知识库 ID、文档 ID、标题、`deleted_slug`、所属知识库、删除时间、清理时间、来源任务和清理任务。墓碑不参与正文预览，但可以在删除记录和任务审计中查询。

#### FR-RET-004 清理任务

- 每日备份完成后执行一次，也可在应用启动后补执行；
- 清理前计算受影响版本、资源和预计释放空间；
- 清理过程可重入，容器重启后不会重复破坏引用；
- 记录清理版本数、资源数、失败数和释放字节数。

### 10.10 搜索和下载

#### FR-SEARCH-001 搜索

- 搜索知识库名称、文档标题、当前 slug、原语雀路径和删除前路径；
- 支持按知识库和删除状态筛选；
- 不索引正文、附件文本或历史版本正文；
- 搜索结果指向当前文档或删除文档最后副本。

#### FR-DOWNLOAD-001 单篇下载

历史版本页面分别提供：

- API 原始响应 JSON；
- 原始正文，扩展名按 `markdown/html/lake/lakesheet` 等格式选择；
- 资源路径已本地化的离线 HTML；
- 每个已成功保存的附件或图片。

MVP 不生成整篇资源 ZIP 或整库 ZIP。离线 HTML 应引用同一服务器上的本地资源；用户下载 HTML 后如果脱离资源目录单独打开，不承诺附件内嵌。

## 11. Web 信息架构

### 11.1 初始化与登录

- 未初始化：创建管理员；
- 已初始化：登录；
- 登录后不显示注册入口或产品营销页。

### 11.2 仪表盘

显示：当前/下一计划时间、最后成功时间、当前任务、等待额度凭据、成功/部分成功/失败数量、选中知识库数量、文档数、版本数和存储用量。问题优先于装饰性统计。

### 11.3 知识库浏览

- 左侧知识库和官方目录树；
- 主区域显示文档列表或文档预览；
- 标识部分成功、已删除、连接停用和无法预览类型；
- 支持标题/路径搜索；
- 目录中的失效链接不能导致页面崩溃。

### 11.4 文档与历史

- 显示当前最新成功版本；
- 历史按本地备份时间倒序；
- 每个版本显示远端更新时间、来源任务、完整性、资源数量和问题数量；
- 可切换版本、查看问题并执行单篇下载；
- 删除文档显示剩余保留时间。

### 11.5 备份任务

- 按时间、状态、触发方式、凭据和知识库筛选；
- 总任务可展开凭据子任务、知识库、文档和资源问题；
- 等待额度显示下次尝试时间；
- 支持取消；
- 历史任务只读，不允许篡改状态。

### 11.6 凭据与设置

- 凭据：新增、验证、更新、启停、删除、知识库发现、主凭据选择；
- 调度：Cron、时区和下次运行预览；
- 存储：路径只读展示、用量、附件上限；
- 保留：天数和清理说明；
- 账户：修改管理员密码；
- 不提供 S3、WebDAV、通知、用户管理或语雀写回设置。

## 12. 状态模型

### 12.1 总任务状态

| 状态 | 含义 | 终态 |
| --- | --- | --- |
| 排队中 | 等待当前总任务完成 | 否 |
| 运行中 | 至少一个子任务正在执行 | 否 |
| 等待额度 | 所有未完成工作都在等待额度；如仍有其他凭据工作则总任务保持运行中 | 否 |
| 成功 | 所有目标完成，无资源或文档问题 | 是 |
| 部分成功 | 有可用正文提交，但存在文档/资源失败或凭据子任务失败 | 是 |
| 失败 | 没有形成可用成果，或发生全局存储/系统错误 | 是 |
| 已取消 | 管理员取消，已提交成果保留 | 是 |

### 12.2 凭据状态

`未验证`、`有效`、`等待额度`、`需要处理`、`已停用`。401/403 进入“需要处理”，429 进入“等待额度”，成功响应恢复为“有效”。

### 12.3 版本完整性

| 状态 | 判定 |
| --- | --- |
| 完整 | 正文和所有计划内资源成功保存 |
| 部分成功 | 正文可用，但一个或多个资源/特殊结构不完整 |
| 失败 | 正文未能形成可提交版本 |

部分成功版本可以成为最新可浏览版本，但界面必须持续显示不完整标识。失败版本不能替换最新成功或部分成功版本。

## 13. 概念数据模型

### 13.1 实体

| 实体 | 职责 | 关键字段 |
| --- | --- | --- |
| `Admin` | 唯一本地管理员 | `id`、`username`、`password_hash`、`created_at`、`password_changed_at` |
| `YuqueCredential` | 一组语雀身份和额度状态 | `id`、`name`、`base_url`、`encrypted_token`、`subject_id`、`subject_type`、`login`、`status`、`enabled`、额度快照、`next_retry_at` |
| `Repository` | 去重后的知识库 | `id`、`base_url`、`book_id`、`slug`、`namespace`、`name`、`selected`、`connection_status`、`content_updated_at`、安全水位、当前 TOC |
| `RepositoryCredential` | 知识库与可访问凭据关系 | `repository_id`、`credential_id`、`is_primary`、`last_discovered_at`、权限/验证状态 |
| `Document` | 稳定文档身份和当前指针 | `id`、`repository_id`、`doc_id`、`type`、`title`、`slug`、目录路径、`deleted_at`、`latest_successful_version_id` |
| `DocumentVersion` | 一次内容快照 | `id`、`document_id`、远端版本 ID、格式、原始响应路径、原始正文路径、离线 HTML 路径、规范化哈希、完整性、来源任务、时间字段 |
| `Asset` | 去重的物理资源 | `id`、`sha256`、`size`、`mime_type`、存储路径、创建时间、引用数 |
| `VersionAsset` | 版本与资源的引用及原位置 | `version_id`、`asset_id`、原始 URL、名称、资源类型、正文位置、下载状态 |
| `BackupJob` | 一次总任务 | `id`、触发方式、目标范围、状态、开始/结束时间、计数、取消时间 |
| `BackupSubtask` | 按凭据/知识库拆分的执行单元 | `id`、`job_id`、`credential_id`、`repository_id`、状态、额度等待、计数 |
| `BackupIssue` | 可追踪的问题 | 归属任务/文档/版本/资源、分类、安全 URL、错误码、摘要、尝试次数、首次/最后时间 |
| `SyncCheckpoint` | 断点续传和幂等 | 请求/文档/资源标识、阶段、完成状态、安全水位、`next_retry_at` |
| `RetentionPolicy` | 全局保留配置 | `retention_days`、`max_asset_size`、更新时间 |
| `DeletionTombstone` | 内容清理后的永久审计 | 域名、知识库 ID、文档 ID、标题、原路径、删除/清理时间、任务 ID |

### 13.2 唯一性与去重规则

- 知识库唯一键：规范化 `base_url + book_id`；
- 文档唯一键：`repository_id + doc_id`；
- 版本去重键：`document_id + normalized_content_hash`；
- 物理资源唯一键：`sha256`；
- 同一任务的相同范围触发使用幂等键合并；
- 不以标题、slug、文件名或原始 URL 作为唯一身份。

### 13.3 建议存储方案

- SQLite 保存管理员、凭据元数据、知识库、文档、版本索引、任务、问题、检查点和引用关系；
- 内容寻址的本地文件目录保存原始响应、原始正文、离线 HTML 和二进制资源；
- 数据库和内容根目录均映射到 Docker 持久卷或宿主机目录；
- NAS 由宿主机完成挂载，应用只看到普通本地路径；
- MVP 不引入 PostgreSQL、Redis、消息队列服务或对象存储；持久化任务队列由 SQLite 实现。

## 14. 异常与结果规则

| 场景 | 系统行为 | 结果状态 |
| --- | --- | --- |
| Token 无效/过期 | 停止对应凭据，提示更新；其他凭据继续 | 子任务失败，总任务部分成功或失败 |
| 空间域名错误 | 验证失败，保留配置供修改 | 凭据需要处理 |
| 429 | 持久化等待并自动探测 | 等待额度 |
| 网络/408/5xx | 按 2s/10s/30s 重试，耗尽后记录问题 | 视其他成果为部分成功或失败 |
| 文档详情 404 | 核对删除视图；否则记录来源不一致 | 删除或单项失败 |
| 图片/附件超限 | 不下载，保留正文和资源元数据 | 部分成功 |
| 图片/附件无权限 | 不使用 Cookie 绕过，记录权限问题 | 部分成功 |
| Sheet/Table 解析失败 | 保存原始正文/分页结果和下载入口 | 部分成功 |
| HTML 含脚本 | 移除危险内容并记录清理，不执行 | 可成功；若正文不可用则部分成功 |
| 磁盘不足 | 停止原子提交，保留旧版本 | 总任务失败或部分成功 |
| 容器重启 | 从持久化队列、临时文件状态和检查点恢复 | 原任务继续 |
| 管理员取消 | 安全停止，保留已提交数据和检查点 | 已取消 |
| 删除文档过期 | 清理内容，保留墓碑和审计 | 清理成功/失败单独记录 |

## 15. 非功能需求

### 15.1 安全

- 不实现任何语雀写请求；HTTP 客户端应通过允许的方法白名单限制为 GET；
- Token 加密保存、传输和日志脱敏；
- 本地下载接口必须检查管理员会话，不能因知道文件路径而匿名读取；
- 防止路径穿越，远端文件名不能直接作为存储路径；
- HTML 清理规则必须有恶意样本测试；
- 外部资源下载需要限制协议、重定向次数、超时和私网访问，防止 SSRF；
- SQLite 和内容目录不得由 Web 服务器直接静态暴露。

### 15.2 可靠性

- 所有版本和资源使用临时写入加原子提交；
- 队列、退避时间、水位和清理引用持久化；
- 重复运行和重启必须幂等；
- 清理和同步不能并发破坏相同文档或资源；
- 数据库事务和文件提交失败时必须可修复，不能产生指向不存在文件的“成功版本”。

### 15.3 性能与容量

- 设计目标为至少 10,000 篇文档和 100,000 个本地版本；
- 标题/路径搜索和目录展开在该规模下正常索引查询目标为 2 秒内返回；
- 大文件必须流式处理；
- 资源哈希和文件移动不得阻塞 API 调度循环；
- 仪表盘统计使用聚合查询或缓存，不能每次扫描内容目录。

### 15.4 可观测性

- 日志包含任务、子任务、凭据匿名标识、知识库和文档 ID；
- 日志不包含 Token、Cookie 或私有请求头；
- 任务计数至少包含：发现、跳过、变化、完整、部分、失败、删除、资源成功/失败；
- 管理员能从 UI 获取足够错误摘要，无需进入容器读取日志才能判断备份是否可信。

### 15.5 可维护性

- 语雀 API 客户端、内容解析、资源下载、预览转换和存储互相隔离；
- 未公开兼容逻辑位于可关闭的适配器，不污染官方客户端；
- 数据库升级必须有版本号和迁移机制；
- Docker Compose 升级前后保持挂载目录兼容，并在不兼容时阻止启动而不是静默重建。

## 16. 验收标准

### AC-01 初始化与认证

1. 空数据库首次访问只能进入管理员初始化；
2. 成功创建后刷新或并发访问均不能再次创建管理员；
3. 错误密码不能登录，正确密码可登录和退出；
4. 修改密码后其他会话失效；
5. 页面源码、接口响应和日志中均找不到完整语雀 Token。

### AC-02 凭据和知识库发现

1. 有效个人 Token 能识别当前用户，并发现个人和有权限的团队知识库；
2. 有效团队 Token 能识别团队并发现其知识库；
3. 无效 Token、无权限、错误空间域名和 429 显示不同状态；
4. 新发现知识库默认全选，可被排除；
5. 两个凭据发现相同域名和 `book_id` 时只显示一个知识库，并要求指定主凭据。

### AC-03 首次备份

1. 保存知识库详情、目录、当前文档和可获取资源；
2. 不批量导入部署前的 100 个历史版本；
3. 每篇文档形成一个可审计的本地当前版本；
4. 任务页面显示准确的文档和资源计数；
5. 容器中断后重启可从最后检查点继续。

### AC-04 增量与幂等

1. 第二次无变化任务不重新获取全部文档详情；
2. 无变化任务不生成重复版本和资源；
3. 修改标题、slug、正文、目录位置或资源后更新同一 `doc_id`；
4. `changed_at_gte` 边界重叠不会生成重复版本；
5. 未完成变化已进入持久化重试队列前，安全水位不会越过它。

### AC-05 删除和保留

1. 官方删除视图中的文档进入已删除状态，并显示 `deleted_at`、原路径和预计清理时间；
2. 默认 15 天内仍可浏览和下载最后副本；
3. 到期后正文及独占资源被清理；
4. 墓碑、来源任务和清理记录仍可查询；
5. 活动文档最新成功版本不会因超过 15 天被清理；
6. 被其他版本引用的共享资源不会误删。

### AC-06 限流、失败与续传

1. 模拟 429 后对应凭据进入等待额度，其他凭据继续；
2. 重启容器后仍按持久化 `next_retry_at` 继续；
3. 额度探测成功后自动恢复，无需管理员重新创建任务；
4. 网络、408 和 5xx 按规定次数和间隔重试；
5. 401/403 停止对应凭据并标记需要处理；
6. 取消任务后已提交版本可用，重新执行从检查点继续。

### AC-07 资源与部分成功

1. 图片和附件以流式方式下载并计算 SHA-256；
2. 相同内容只保存一个物理文件；
3. 超过默认 500 MB 的资源不下载并生成问题；
4. 无权限、过期或失败资源不会使已取得正文丢失；
5. 文档和任务显示部分成功，并可查看资源级原因；
6. 系统不会要求或使用浏览器 Cookie。

### AC-08 六种文档类型

1. `Doc` 和 `HtmlDoc` 可安全预览；
2. `Thread` 和 `Board` 可用 HTML 时预览，不可用时显示原始下载和告警；
3. `Sheet` 多工作表可只读切换；
4. `Table` 超过 200 行时能分页取得全部记录；
5. 特殊类型解析失败时保存原始数据且不显示空白成功页；
6. 恶意脚本、事件属性和危险 URL 不能执行。

### AC-09 调度和任务记录

1. 默认 Cron 在 `Asia/Shanghai` 每天 02:00 触发；
2. 修改 Cron/时区后展示正确的后三次执行时间；
3. 手动任务可选择全部、凭据或知识库；
4. 同时只运行一个总任务；重复触发按范围合并；
5. 任务可按时间、状态、触发方式、凭据和知识库筛选；
6. 状态只使用排队中、运行中、等待额度、成功、部分成功、失败、已取消。

### AC-10 离线浏览和下载

1. 阻断系统访问语雀后，已备份知识库目录仍可打开；
2. 标题和路径搜索仍可使用；
3. 历史版本仍可切换和预览；
4. API 原始响应、原始正文、离线 HTML 和已保存附件可分别下载；
5. 下载接口要求有效管理员会话；
6. 不存在整库 ZIP 或恢复回语雀入口。

### AC-11 持久化和磁盘故障

1. Docker Compose 重启后管理员、凭据、选择范围、任务、版本、问题和资源均存在；
2. 模拟磁盘空间不足时不产生指向缺失文件的成功版本；
3. 最后成功版本仍可浏览；
4. 清理释放空间后可重新执行任务；
5. NAS 作为宿主机挂载目录时行为与普通本地路径一致。

## 17. 明确排除项

MVP 不包含：

- 创建、更新、删除或恢复语雀文档；
- 双向同步、冲突合并或本地编辑器；
- 整库 ZIP、自动迁移或灾难恢复写回；
- 部署前历史版本回补；
- 正文全文搜索、OCR 或附件内容索引；
- 评论、评审、点赞、阅读统计的完整备份；
- Webhook 增量和公网回调；
- 邮件、短信、企业微信、钉钉等通知；
- S3、WebDAV 或其他对象存储后端；
- 多本地用户、RBAC、SSO、LDAP 或知识库级本地授权；
- AI、RAG、知识图谱、聊天问答；
- 移动客户端或桌面客户端；
- 使用浏览器 Cookie、账号密码登录语雀或绕过权限；
- 保证附件、画板内部对象、第三方 iframe 或外链内容完全离线化。

## 18. 风险与应对

| 风险 | 影响 | MVP 应对 |
| --- | --- | --- |
| 语雀调整接口或会员权益 | 凭据失效、字段变化 | 启动/任务前验证；保存原始响应；API 客户端隔离 |
| 共享额度低于预期 | 长时间备份 | 单请求通道、响应头感知、持久化 429 等待、增量查询 |
| 附件无官方接口 | 资源不完整 | 正文优先、URL 解析、兼容适配器、部分成功和明确告警 |
| 特殊文档格式变化 | 预览失败 | 保存原始字段、按类型解析、可下载降级 |
| 本地磁盘增长 | 任务失败或占满主机 | 15 天保留、SHA-256 去重、500 MB 上限、用量展示、原子提交 |
| HTML 主动内容 | 本地 XSS/数据外传 | 严格清理、禁止脚本和危险 URL、资源本地化 |
| 多凭据重复访问 | 浪费额度和重复数据 | 域名+book_id 去重、管理员指定主凭据 |
| 清理误删共享资源 | 历史版本损坏 | 显式引用表、事务清理、引用为零才删除 |
| 无许可证代码被误用 | 法律风险 | 禁止复制 yuque-dl；复用登记和许可证扫描 |

## 19. 已确认默认值与假设

| 项目 | 已确认值 |
| --- | --- |
| 本地用户 | 唯一管理员，首次启动向导创建 |
| 语雀凭据 | 支持多组 |
| 知识库选择 | 自动发现、默认全选、允许排除 |
| 重复库 | 管理员指定主凭据 |
| 调度 | 全局五段 Cron + 手动触发 |
| 默认 Cron | `0 2 * * *` |
| 默认时区 | `Asia/Shanghai` |
| 首次历史 | 只备份当前版本，不回补历史 |
| 保留期 | 默认 15 天，可修改 |
| 删除内容 | 删除后按保留期清理，永久保留墓碑 |
| 单资源上限 | 默认 500 MB，可修改或设为无限制 |
| 搜索 | 知识库名称、标题和路径，不做全文搜索 |
| 恢复 | 本地预览和下载，不回写语雀 |
| 下载 | 原始响应/正文、离线 HTML、单个附件 |
| 存储 | SQLite + 本地内容目录；Docker 挂载持久化 |
| 外部存储 | 不支持 S3/WebDAV；NAS 由宿主机挂载 |
| 资源失败 | 正文保留，版本和任务标记部分成功 |

本需求没有遗留的阻塞性产品决策。后续如需扩大范围，应创建独立版本需求，不得在 MVP 开发中默认加入排除项。

## 20. 来源

除非条目另有说明，以下官方页面、竞品资料和开源仓库的检索、抓取及交叉核验日期均为 `2026-07-23`；链接用于标识事实来源，不代表第三方能力属于语雀官方承诺。

### 20.1 语雀官方资料

1. [语雀开放 API 接口文档](https://www.yuque.com/yuque/developer/openapi)  
   用途：当前 OAS 下载入口、调用频率、额度响应头、共享额度、空间子域名和 URL 概念。页面更新时间 2026-07-22。
2. `yuque_openapi_20260722.yaml`（从上述官方页面登录后下载，112 KB）  
   用途：OpenAPI 3.1.0 完整路径、参数、数据结构、错误码、删除查询、增量查询、历史版本、文档类型和数据表格式。
3. [Overview](https://www.yuque.com/yuque/developer/api)  
   用途：`X-Auth-Token`、个人超级会员权益、团队旗舰版权益、权限范围和异常使用警告。
4. [语雀开放 API 访问范围变更](https://www.yuque.com/yuque/developer/vzippmige58g7r9t)  
   用途：2022-04-10 后 Token 只能访问有权限内容。
5. [使用 API 获取空间内文档](https://www.yuque.com/yuque/developer/gyht993a76zg54mv)  
   用途：空间/团队 Token、旗舰版限制和知识库到文档的读取流程。
6. [文档 HTML 格式说明](https://www.yuque.com/yuque/developer/yr938f)  
   用途：HTML 正文、图片 URL 和本地代码高亮责任。
7. [Webhooks 消息推送](https://www.yuque.com/yuque/developer/doc-webhook)  
   用途：评估 Webhook 的公网要求和触发限制；最终不纳入 MVP。

### 20.2 竞品与行业资料

1. [ProBackup - Notion Backup & Restore](https://www.probackup.io/backup/notion)
2. [Rewind Backups - Notion](https://rewind.com/products/backups/notion/)
3. [SimpleBackups - Notion Workspace Backup](https://simplebackups.com/blog/how-to-back-up-notion-workspace)
4. [Notion Help - Back up your data](https://www.notion.com/help/back-up-your-data)

竞品资料只用于提炼产品模式，不作为语雀 API 行为依据。

### 20.3 开源项目

1. [gxr404/yuque-dl](https://github.com/gxr404/yuque-dl)
2. [atian25/yuque-exporter](https://github.com/atian25/yuque-exporter)
3. [M1r0ku/YuqueExport](https://github.com/M1r0ku/YuqueExport)
4. [Tencent/WeKnora](https://github.com/Tencent/WeKnora)
5. [yuque-helper/yuque2book](https://github.com/yuque-helper/yuque2book)

开源项目的功能和许可证结论以 2026-07-23 的仓库状态为准；实际复用时必须重新固定提交并复核许可证。
