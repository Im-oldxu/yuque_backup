# Yuque Backup 前端快速启动说明

## 1. 项目与运行要求

前端位于 `frontend/`，是在 `tabtab-dev/tabtab-admin` 固定提交的 Vue 3 单应用底座上开发的 Yuque Backup 管理端。项目使用 Vue 3、TypeScript、Vite/Rolldown、Tailwind CSS v4、shadcn-vue/Reka UI、Pinia、Vue Router 和 Vitest。

v1.1.0 自动化交付基线已实际验证：17 个 Vitest 测试文件、61 个测试、Vue/TypeScript 类型检查和生产构建均通过。v1.0.0 的真实 API 浏览器联调覆盖初始化、登录、退出、会话 Cookie、CSRF、仪表盘和 worker 在线状态；v1.1.0 沿用相同的 API 代理与会话机制。

运行环境：

- Node.js `^20.19.0` 或 `>=22.12.0`；
- pnpm `10.29.1`，已写入 `frontend/package.json`；
- Mock 模式不需要启动后端。

建议使用 Corepack 调用项目锁定的 pnpm：

```powershell
corepack enable
```

## 2. 安装

在项目根目录执行：

```powershell
cd frontend
corepack pnpm install --frozen-lockfile
```

这是单应用工程，不需要构建 workspace 包，也不需要安装或启动任何后端代码。

## 3. 配置

环境变量示例位于 `frontend/.env.example`。默认开发模式需要覆盖时，创建 `frontend/.env.development.local`；显式真实接口模式需要覆盖时，创建 `frontend/.env.real.local`。

### 3.1 Mock 模式

默认开发配置已经启用内置 Mock：

```dotenv
VITE_API_MODE=mock
VITE_API_BASE_URL=/api/v1
VITE_MOCK_DELAY=120
VITE_MOCK_FORCE_ERROR=false
```

Mock 只模拟《API 接口文档.md》已定义的路径、字段、分页、错误和业务约束。页面代码仍通过同一 `api` 模块调用，切换真实接口时不需要修改页面。

需要集中检查异常状态时，可临时设置 `VITE_MOCK_FORCE_ERROR=true`；除初始化状态外的 Mock 请求会统一返回文档定义的 `503 SERVICE_UNAVAILABLE`。修改后需要重启开发服务器。

重新验证首次初始化流程时，在浏览器开发者工具中执行：

```javascript
localStorage.removeItem('yb_mock_admin_v1')
localStorage.removeItem('yb_mock_initialized')
localStorage.removeItem('yb_mock_admin')
sessionStorage.removeItem('yb_mock_session_version')
sessionStorage.removeItem('yb_mock_authenticated')
location.reload()
```

### 3.2 真实 API 模式

后端可用后，在 `frontend/.env.real.local` 中设置：

```dotenv
VITE_API_MODE=real
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
```

然后使用真实接口模式启动：

```powershell
corepack pnpm dev:real
```

开发服务器会把 `/api` 代理到 `VITE_API_PROXY_TARGET`。真实请求统一使用 `credentials: include`，并按接口文档携带 `yb_session`、`yb_csrf` Cookie、`X-CSRF-Token` 和幂等键。

后端的 `TRUSTED_ORIGINS` 必须包含浏览器实际 Origin。使用默认端口时，后端开发配置至少应包含：

```dotenv
TRUSTED_ORIGINS=http://127.0.0.1:3002,http://localhost:3002
SECURE_COOKIES=false
```

生产部署应让前端与 `/api/v1` 同源，或者采用能够正确处理凭据 Cookie、CSRF 和下载鉴权的反向代理配置。不要把文档预览或下载目录作为匿名静态目录暴露。

## 4. 启动

启动开发服务器：

```powershell
corepack pnpm dev
```

默认地址：`http://127.0.0.1:3002/#/auth`

也可以显式使用 Mock 模式启动：

```powershell
corepack pnpm dev:mock
```

主要页面：

- `/#/auth`：首次初始化或管理员登录；
- `/#/dashboard`：仪表盘和手动备份入口；
- `/#/credentials`：凭据新增、更新、验证、发现、启停和删除；
- `/#/repositories`：知识库、官方目录、主凭据和文档搜索；
- `/#/documents/:documentId`：可折叠阅读目录、Markdown 大纲、历史版本、安全预览、资源、问题和下载；
- `/#/jobs`：任务筛选、分页、创建、合并、取消、重跑、子任务和问题；
- `/#/settings`：调度、保留、存储和管理员密码；
- `/#/tombstones`：已清理文档的永久审计墓碑。

## 5. 测试与类型检查

运行全部单元测试：

```powershell
corepack pnpm test
```

监听模式：

```powershell
corepack pnpm test:watch
```

运行 Vue 与 TypeScript 类型检查：

```powershell
corepack pnpm typecheck
```

测试覆盖率报告：

```powershell
corepack pnpm test:coverage
```

## 6. 构建与预览

生产构建默认使用 `frontend/.env.production` 的真实 API 模式：

```powershell
corepack pnpm build
```

同时执行类型检查和生产构建：

```powershell
corepack pnpm build:check
```

需要生成可脱离后端演示的 Mock 构建时：

```powershell
corepack pnpm build:mock
```

构建产物位于 `frontend/dist/`。本地预览：

```powershell
corepack pnpm preview --host 127.0.0.1 --port 4173
```

应用使用 Hash Router，因此静态服务器无需为业务路由额外配置 history fallback。

## 7. 建议验收流程

1. 清除浏览器站点存储，打开 `/#/auth`，创建唯一管理员；用户名至少 3 个字符，密码至少 12 个字符。
2. 退出后分别用错误密码和正确密码登录，再到设置页验证修改密码。
3. 在凭据页新增凭据，等待验证 Operation 完成；继续执行更新、发现知识库、停用、启用和删除。
4. 在知识库页检查列表分页、官方目录、主凭据、选择开关和文档搜索。
5. 打开文档，折叠和展开阅读目录，在知识库目录与文章大纲之间切换；点击大纲标题并切换顶部历史版本。
6. 检查预览 iframe 不包含 `allow-scripts` 或 `allow-same-origin`，再验证资源、问题和下载状态。
7. 在任务页组合状态、触发方式、凭据、知识库和时间筛选；创建全部、凭据和知识库范围任务，并检查合并、取消、重跑和详情。
8. 在设置页保存 Cron、时区、保留天数和资源大小限制，并检查空数据、加载失败及保存失败提示。
9. 使用桌面和移动端视口检查侧栏、阅读目录抽屉、表格横向滚动、对话框和文字无重叠。

## 8. 常见问题

### 页面始终停留在登录页

Mock 模式下先清除当前站点的 `localStorage` 和 `sessionStorage` 后重新初始化。真实模式下检查 API 同源代理、`yb_session` Cookie 以及 `/api/v1/auth/me` 响应。

### 修改环境变量后页面没有变化

Vite 只在启动时读取环境变量。停止开发服务器后重新执行 `corepack pnpm dev`。

### 修改请求返回 CSRF 错误

真实模式必须先由后端设置 `yb_csrf` Cookie。前端会读取该 Cookie，并在非 GET 请求中发送 `X-CSRF-Token`。检查 Cookie 的域、路径、SameSite 和反向代理配置。

### 依赖安装提示 Node 版本不支持

Rolldown-Vite `7.3.1` 要求 Node.js `^20.19.0` 或 `>=22.12.0`。升级 Node 后删除旧 `node_modules`，再执行 `corepack pnpm install --frozen-lockfile`。

## 9. 上游来源

新 UI 底座来源、固定提交与许可证说明见 `frontend/UPSTREAM.md`。上游 MIT `LICENSE` 已保留在前端目录。
